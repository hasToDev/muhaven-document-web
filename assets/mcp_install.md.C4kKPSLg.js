import{c as e,Q as a,j as i,m as t}from"./chunks/framework.BPKcPtvA.js";const k=JSON.parse('{"title":"@muhaven/mcp — install","description":"From npm install to a logged-in broker in three commands.","frontmatter":{"title":"@muhaven/mcp — install","description":"From npm install to a logged-in broker in three commands."},"headers":[],"relativePath":"mcp/install.md","filePath":"mcp/install.md","lastUpdated":1779067309000}'),n={name:"mcp/install.md"};function o(l,s,p,h,r,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="install-muhaven-mcp" tabindex="-1">Install <code>@muhaven/mcp</code> <a class="header-anchor" href="#install-muhaven-mcp" aria-label="Permalink to &quot;Install \`@muhaven/mcp\`&quot;">​</a></h1><p>Three steps from a fresh machine to a working MCP install:</p><ol><li>Install the package globally.</li><li>Start the broker and complete the device-code login.</li><li>Wire your host&#39;s MCP config and restart.</li></ol><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Node 20 or later</strong> (for the broker daemon).</li><li><strong>A MuHaven account</strong> with a passkey-bound MuHaven wallet (sign up on <code>muhaven.app</code>).</li><li><strong>An MCP-aware host:</strong> Claude Code, Claude Desktop, or Cursor.</li></ul><h2 id="step-1-—-install-globally" tabindex="-1">Step 1 — Install globally <a class="header-anchor" href="#step-1-—-install-globally" aria-label="Permalink to &quot;Step 1 — Install globally&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @muhaven/mcp</span></span></code></pre></div><p>This installs two binaries:</p><ul><li><code>muhaven-mcp</code> — the STDIO subprocess your LLM host spawns.</li><li><code>muhaven-broker</code> — the long-lived per-user daemon.</li></ul><p>Verify the binaries are on your PATH:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --help</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-mcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --help</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">On Windows / WSL2 / SSH-remote: set <code>MUHAVEN_KEYRING=file</code></p><p>The default keyring uses your OS keychain (Keychain on macOS, Secret Service on Linux, Credential Manager on Windows). On WSL2, devcontainers, and SSH-remote shells, that&#39;s often unavailable. Set <code>MUHAVEN_KEYRING=file</code> to use a 0600-mode file at <code>~/.muhaven/jwt.json</code> instead.</p></div><h2 id="step-2-—-one-shot-setup-with-auto-register" tabindex="-1">Step 2 — One-shot setup with auto-register <a class="header-anchor" href="#step-2-—-one-shot-setup-with-auto-register" aria-label="Permalink to &quot;Step 2 — One-shot setup with auto-register&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> setup</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --register</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> claude-code</span></span></code></pre></div><p>That one command does everything:</p><ol><li><strong>Env defaults</strong> — <code>MUHAVEN_BACKEND_URL=https://api.muhaven.app</code> and <code>MUHAVEN_DASHBOARD_URL=https://muhaven.app</code> are applied when unset. On Windows, WSL2, devcontainers, Codespaces, and SSH-remote shells, <code>MUHAVEN_KEYRING=file</code> is auto-applied too (the OS keychain is typically unavailable or unwise on those surfaces). Native macOS / Linux desktop leaves the keyring on the default (OS keychain).</li><li><strong>Session key</strong> — mints an ephemeral 32-byte key into <code>MUHAVEN_BROKER_SESSION_KEY</code> if not already present.</li><li><strong>Daemon</strong> — spawns the broker daemon detached in the background. Idempotent: if a daemon is already running, this step is skipped.</li><li><strong>Device-code login</strong> — opens your default browser to <code>https://muhaven.app/link?code=ABCD-1234</code>. Verify the requesting client metadata (name, scopes, fingerprint) matches the broker you just started, then approve with your passkey. The broker receives a scoped JWT (<code>mcp.read.*</code> + <code>mcp.propose.*</code>) and stores it in your OS keychain (or <code>~/.muhaven/jwt.json</code> with file keyring).</li><li><strong>Host register</strong> <em>(new in 0.1.6, requires <code>--register HOST</code> flag)</em> — shells out to <code>claude mcp add-json muhaven &#39;&lt;json&gt;&#39; --scope user</code>. Removes any prior <code>muhaven</code> entry first (idempotent). Default scope is <code>user</code> so every project on this machine sees the server. Pass <code>--register-scope project</code> to write <code>.mcp.json</code> in the current directory instead, or <code>--register-scope local</code> for per-project user-only entries.</li><li><strong>Closing summary</strong> — prints the daemon PID + endpoint + the command to stop it later.</li></ol><div class="tip custom-block"><p class="custom-block-title">Why <code>--register</code> instead of editing <code>.mcp.json</code> by hand?</p><p>Pre-0.1.6 the operator had to <code>cat &gt; .mcp.json</code> with a hand-rolled JSON block. <code>--register claude-code</code> calls the official <code>claude mcp add-json</code> CLI, so the host config gets the same shape Claude Code&#39;s own tooling writes. Reserved host names (<code>claude-desktop</code>, <code>cursor</code>) parse but print a &quot;not implemented&quot; hint — file-edit registrars for those hosts land in a follow-up release.</p></div><p>The login JWT is currently <strong>1 hour</strong>. After expiry, run <code>muhaven-broker setup</code> again (or just <code>muhaven-broker login</code> if the daemon is still running).</p><div class="tip custom-block"><p class="custom-block-title">Supervised daemon? Use <code>--foreground</code></p><p>If systemd / launchd / a process supervisor will own the broker daemon&#39;s lifecycle, run <code>muhaven-broker setup --foreground</code> (or <code>-f</code>) — that path applies env defaults + mints the session key, then runs the daemon attached so the supervisor can see its stderr + handle restarts. The login step is skipped in foreground mode (run <code>muhaven-broker login</code> afterward from a second shell).</p></div><p>Verify:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span></code></pre></div><p>Outputs your bound wallet address, the scopes on your JWT, the keystore location, and the broker socket path.</p><h3 id="what-if-i-want-to-do-this-manually" tabindex="-1">What if I want to do this manually? <a class="header-anchor" href="#what-if-i-want-to-do-this-manually" aria-label="Permalink to &quot;What if I want to do this manually?&quot;">​</a></h3><p>The five-step ritual <code>setup</code> replaces is still available:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal A</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_BROKER_SESSION_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0x$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;process.stdout.write(require(&#39;crypto&#39;).randomBytes(32).toString(&#39;hex&#39;))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_KEYRING</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">file                            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># if on Windows / WSL2 / SSH</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_BACKEND_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://api.muhaven.app</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_DASHBOARD_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://muhaven.app</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                          # leave running</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal B</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> login</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                    # passkey ceremony</span></span></code></pre></div><h2 id="step-3-—-wire-your-host" tabindex="-1">Step 3 — Wire your host <a class="header-anchor" href="#step-3-—-wire-your-host" aria-label="Permalink to &quot;Step 3 — Wire your host&quot;">​</a></h2><h3 id="claude-code" tabindex="-1">Claude Code <a class="header-anchor" href="#claude-code" aria-label="Permalink to &quot;Claude Code&quot;">​</a></h3><p><strong>If you ran <code>muhaven-broker setup --register claude-code</code> in Step 2, you&#39;re done.</strong> Skip to &quot;Verify end-to-end&quot; below.</p><p>If you want to register manually (or your <code>--register</code> step printed a warning), use the official <code>claude mcp</code> CLI:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add-json</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> muhaven</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;type&quot;:&quot;stdio&quot;,&quot;command&quot;:&quot;muhaven-mcp&quot;,&quot;env&quot;:{&quot;MUHAVEN_BACKEND_URL&quot;:&quot;https://api.muhaven.app&quot;,&quot;MUHAVEN_DASHBOARD_URL&quot;:&quot;https://muhaven.app&quot;}}&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --scope</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user</span></span></code></pre></div><p>The CLI writes the entry into <code>~/.claude.json</code>. <code>--scope project</code> writes <code>.mcp.json</code> in the current directory instead (committed to your team&#39;s repo); <code>--scope local</code> writes a per-project user-only entry (Claude Code&#39;s default).</p><div class="tip custom-block"><p class="custom-block-title">Why the official CLI instead of editing JSON by hand?</p><p>The <code>claude mcp add-json</code> command shipped in 2025-Q4 alongside MCP Elicitation support and is the supported way to register a server. Hand-edited <code>.mcp.json</code> files still work, but the CLI handles scope semantics + idempotency + the future migration to URL-elicitation flows cleanly.</p></div><p>Restart Claude Code. The next time you start a chat, <code>muhaven</code> appears in the MCP server list and the 23 tools are available.</p><h3 id="claude-desktop" tabindex="-1">Claude Desktop <a class="header-anchor" href="#claude-desktop" aria-label="Permalink to &quot;Claude Desktop&quot;">​</a></h3><p>Edit <code>~/Library/Application Support/Claude/claude_desktop_config.json</code> (macOS) or <code>%APPDATA%\\Claude\\claude_desktop_config.json</code> (Windows):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;muhaven&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;muhaven-mcp&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Restart Claude Desktop. The MCP tools surface in the chat composer&#39;s tool dropdown.</p><div class="warning custom-block"><p class="custom-block-title">Claude Desktop on Windows: PATH may not propagate</p><p>Claude Desktop on Windows sometimes runs in a sandbox that doesn&#39;t see your user PATH. If <code>muhaven-mcp</code> isn&#39;t found, use the absolute path: <code>&quot;C:\\\\Users\\\\you\\\\AppData\\\\Roaming\\\\npm\\\\muhaven-mcp.cmd&quot;</code>.</p></div><h3 id="cursor" tabindex="-1">Cursor <a class="header-anchor" href="#cursor" aria-label="Permalink to &quot;Cursor&quot;">​</a></h3><p>Cursor&#39;s MCP config is at <code>~/.cursor/mcp.json</code> (or via the Cursor settings UI → MCP):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;muhaven&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;muhaven-mcp&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Restart Cursor. The Tools panel will list the MuHaven tools.</p><div class="tip custom-block"><p class="custom-block-title">Use the right host-config snippet</p><p>The exact host-config format hasn&#39;t been stress-tested across every Cursor / Claude Desktop release. If your host advertises a slightly different <code>mcpServers</code> shape, follow the host&#39;s docs — the <code>command: &quot;muhaven-mcp&quot;</code> value stays the same.</p></div><h2 id="read-only-mode" tabindex="-1">Read-only mode <a class="header-anchor" href="#read-only-mode" aria-label="Permalink to &quot;Read-only mode&quot;">​</a></h2><p>If you want a &quot;give my LLM read-only visibility&quot; deployment (shared workstation, curated investor dashboard, etc.), set <code>MUHAVEN_READ_ONLY=true</code> in the broker env before starting:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_READ_ONLY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span></span></code></pre></div><p>Only the <strong>seven <code>muhaven.read.*</code> tools</strong> will be registered. Position / policy / issuer / governance groups are not even surfaced to the host LLM — defense in depth. See <a href="/mcp/read-only-mode">Read-only mode</a>.</p><h2 id="multiple-installs-e-g-a-staging-install-for-testing" tabindex="-1">Multiple installs (e.g., a staging install for testing) <a class="header-anchor" href="#multiple-installs-e-g-a-staging-install-for-testing" aria-label="Permalink to &quot;Multiple installs (e.g., a staging install for testing)&quot;">​</a></h2><p>You can run two broker instances side by side:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal A — prod broker</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Terminal B — staging broker on a different socket</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_BACKEND_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://api-stage.muhaven.app</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MUHAVEN_BROKER_SOCKET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/tmp/muhaven-broker-stage.sock</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span></span></code></pre></div><p>Wire each to a different host config entry (e.g., <code>muhaven</code> and <code>muhaven-stage</code> keys in your <code>.mcp.json</code>).</p><h2 id="verify-end-to-end" tabindex="-1">Verify end-to-end <a class="header-anchor" href="#verify-end-to-end" aria-label="Permalink to &quot;Verify end-to-end&quot;">​</a></h2><p>In your host:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; What does my muhaven portfolio look like?</span></span></code></pre></div><p>Should produce a tool call to <code>muhaven.read.portfolio</code> and return your aggregate state. If you get an auth error, run <code>muhaven-broker doctor</code> to inspect your JWT status.</p><h2 id="uninstall" tabindex="-1">Uninstall <a class="header-anchor" href="#uninstall" aria-label="Permalink to &quot;Uninstall&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">muhaven-broker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logout</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                # revokes the broker JWT in keychain</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uninstall</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -g</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @muhaven/mcp</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # removes the binaries</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (optional) rm -rf ~/.muhaven       # removes any file-keyring data</span></span></code></pre></div><p>Your MuHaven wallet itself is on-chain and unaffected by uninstalling MCP — sign in to the dashboard with your passkey and your account is fully intact.</p><h2 id="where-next" tabindex="-1">Where next <a class="header-anchor" href="#where-next" aria-label="Permalink to &quot;Where next&quot;">​</a></h2><ul><li><a href="/mcp/first-chat">First chat</a> — walk through your first portfolio query.</li><li><a href="/mcp/tools">Tool catalog</a> — the 22 tools you have access to.</li><li><a href="/mcp/broker">Broker daemon</a> — what the broker actually does.</li><li><a href="/mcp/troubleshooting">Troubleshooting</a> — common install issues.</li></ul>`,60)])])}const u=e(n,[["render",o]]);export{k as __pageData,u as default};
